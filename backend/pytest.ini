
testpaths = tests

python_files = test_*.py

#settings for asyncio
asyncio_mode = auto
asyncio_default_fixture_loope_scope = function

#special markers for tests
markers = 
    unit: Unit tests(fast,isolated)
    integration: Integration tests(database, external services)
    e2e: End-to-end tests(fullstack,via HTTP)